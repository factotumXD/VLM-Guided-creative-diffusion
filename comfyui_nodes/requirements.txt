# Dependencies for the VLM-Guided ComfyUI custom nodes.
# ComfyUI already ships: torch, torchvision, numpy, PIL/Pillow, transformers,
# accelerate, safetensors, scipy. Only list what is NOT bundled with ComfyUI.

# Required by the Qwen2.5-VL oracle (image preprocessing helpers).
qwen-vl-utils>=0.0.2
